package MainClasses.Users;

public class Teacher extends User {
    public Teacher(String recordNumber, String firstName, String lastName, String surname, String group, String email) {
        super(recordNumber, firstName, lastName, surname, group, email);
    }
}
