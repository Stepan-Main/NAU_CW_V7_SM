package MainClasses.Users;

public class Student extends User {
    public Student(String recordNumber, String firstName, String lastName, String surname, String group, String email) {
        super(recordNumber, firstName, lastName, surname, group, email);
    }
}
